# Используйте официальный образ Golang в качестве основы
FROM golang:latest

# Установка переменной окружения для порта, который будет прослушивать приложение
ENV PORT=8080

# Установка рабочей директории внутри контейнера
WORKDIR /app

# Копирование файла Go в контейнер
COPY . .

# Сборка приложения
RUN go build -o main .

# Указание команды для запуска приложения при запуске контейнера
CMD ["./main"]
